bedlam:
  _target_: dvlt.data.datasets.parser.dataverse.DataverseTrainDataset
  view_ranking: "index"
  view_sampling: "random_ordered"
  max_depth_thresh: 30.0
  window_size_ratio: 10.0
  dataverse_cfg:
    target: "bedlam_tracks.BedlamTracks"
    params:
      # format: h5
      root_path: "${user.data_root}/train/bedlam"
