cubify_anything:
  _target_: dvlt.data.datasets.parser.dataverse.DataverseTrainDataset
  view_ranking: "pose"
  view_sampling: "random"
  window_size_ratio: 2.0
  max_depth_thresh: 100.0
  dataverse_cfg:
    target: "optimized_hdf5.OptimizedHDF5Dataset"
    params:
      data_root: "${user.data_root}/train/cubify_anything"
